# Code generated by sqlc. DO NOT EDIT.
# versions:
#   sqlc v1.30.0
# source: games.sql
from collections.abc import AsyncIterator
import uuid

import sqlalchemy
import sqlalchemy.ext.asyncio

from jetlag.db.generated import models

CREATE_GAME = """-- name: create_game \\:one
INSERT INTO games (name, size)
VALUES (:p1, :p2)
RETURNING id, name, size, phase, current_round_index, created_at
"""


GET_GAME = """-- name: get_game \\:one
SELECT id, name, size, phase, current_round_index, created_at FROM games WHERE id = :p1
"""


LIST_GAMES = """-- name: list_games \\:many
SELECT id, name, size, phase, current_round_index, created_at FROM games ORDER BY created_at DESC
"""


UPDATE_GAME_PHASE = """-- name: update_game_phase \\:exec
UPDATE games SET phase = :p2, current_round_index = :p3 WHERE id = :p1
"""


class AsyncQuerier:
    def __init__(self, conn: sqlalchemy.ext.asyncio.AsyncConnection):
        self._conn = conn

    async def create_game(self, *, name: str, size: models.GameSize) -> models.Game | None:
        row = (await self._conn.execute(sqlalchemy.text(CREATE_GAME), {"p1": name, "p2": size})).first()
        if row is None:
            return None
        return models.Game(
            id=row[0],
            name=row[1],
            size=row[2],
            phase=row[3],
            current_round_index=row[4],
            created_at=row[5],
        )

    async def get_game(self, *, id: uuid.UUID) -> models.Game | None:
        row = (await self._conn.execute(sqlalchemy.text(GET_GAME), {"p1": id})).first()
        if row is None:
            return None
        return models.Game(
            id=row[0],
            name=row[1],
            size=row[2],
            phase=row[3],
            current_round_index=row[4],
            created_at=row[5],
        )

    async def list_games(self) -> AsyncIterator[models.Game]:
        result = await self._conn.stream(sqlalchemy.text(LIST_GAMES))
        async for row in result:
            yield models.Game(
                id=row[0],
                name=row[1],
                size=row[2],
                phase=row[3],
                current_round_index=row[4],
                created_at=row[5],
            )

    async def update_game_phase(self, *, id: uuid.UUID, phase: models.GamePhase, current_round_index: int) -> None:
        await self._conn.execute(sqlalchemy.text(UPDATE_GAME_PHASE), {"p1": id, "p2": phase, "p3": current_round_index})
