#!/usr/bin/env bash
#MISE description="Lint Python with ruff (or fix with --fix)"
#USAGE flag "--fix" help="Apply safe auto-fixes"
set -euo pipefail

if [[ "${usage_fix:-false}" == "true" ]]; then
	uv run ruff check --fix .
else
	uv run ruff check .
fi
