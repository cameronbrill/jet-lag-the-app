#!/usr/bin/env bash
#MISE description="Plan a new Atlas migration from schema changes"
#USAGE arg "<name>" help="Migration name"
set -euo pipefail
cd "$(cd "$(dirname "${BASH_SOURCE[0]}")/../.." && pwd)"
atlas migrate diff "${usage_name}" --env local
