#!/usr/bin/env bash
#MISE description="Clean build artifacts"
set -euo pipefail
rm -rf build/ dist/ *.egg-info .ruff_cache/ .pytest_cache/ .coverage htmlcov/ results/
find . -type d -name __pycache__ -exec rm -rf {} + 2>/dev/null || true
