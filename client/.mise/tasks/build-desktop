#!/usr/bin/env bash
#MISE description="Package desktop app for current OS"
#MISE depends=["test"]
set -euo pipefail
ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/../.." && pwd)"
cd "$ROOT"
exec mise x -- ./gradlew :app:desktopApp:packageDistributionForCurrentOS
